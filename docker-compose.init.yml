services:
  webhook-initializer:
    container_name: webhook-initializer
    build:
      context: webhook-initializer
      dockerfile: Dockerfile
    env_file:
      - src/webhook-initializer/.env

networks:
  default:
    external: true
    name: magento